export function TeacherModel(params) {
    this.id = params.id;
    this.fullName = params.fullName;
    this.qualification = params.qualification;
    this.birthDate = params.birthDate;
    this.imagePath = params.imagePath;
}